@model ColegioSanJose.Models.DB.Materia

@{
    ViewData["Title"] = "Eliminar Materia";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h2 class="mb-4 text-danger">¿Estás seguro que deseas eliminar esta materia?</h2>

            <div class="card shadow-sm">
                <div class="card-body">
                    <h4 class="card-title">Materia</h4>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-4">Nombre de la Materia</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.NombreMateria)</dd>

                        <dt class="col-sm-4">Docente Asignado</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Docente)</dd>
                    </dl>
                </div>
            </div>

            <form asp-action="Delete" method="post" class="mt-4">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="MateriaId" />
                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-danger">Eliminar</button>
                </div>
            </form>
        </div>
    </div>
</div>